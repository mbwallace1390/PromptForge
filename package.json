{
  "name": "promptforge",
  "version": "0.1.0",
  "private": true,
  "description": "Turns a rough idea into a detailed, build-ready AI prompt. Single-file web app.",
  "type": "module",
  "scripts": {
    "serve": "node serve.mjs",
    "test": "node --test test/delivery.mjs && node test/e2e.mjs && node test/ui-audit.mjs && node test/interview-audit.mjs && node test/ai-audit.mjs",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
