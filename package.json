{
  "name": "promptforge",
  "version": "0.1.0",
  "private": true,
  "description": "Guided AI prompts for apps, existing-app changes, and product videos. Single-file web app.",
  "type": "module",
  "scripts": {
    "serve": "node serve.mjs",
    "test": "node --test test/delivery.mjs test/video-content.mjs && node test/e2e.mjs && node test/ui-audit.mjs && node test/interview-audit.mjs && node test/ai-audit.mjs && node test/video.mjs",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
